import styled from "@emotion/styled";

const MainStyled = styled.main`
  position: relative;
`;

const SideBarStyled = styled.aside`
  position: absolute;
`;

const MainContentContainerStyled = styled.div`
  padding: 150px 0;
`;

export { MainStyled, SideBarStyled, MainContentContainerStyled };
